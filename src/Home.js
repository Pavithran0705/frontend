const Home = () => {
    return (
        <div className="container mt-4">
            <h2>Welcome to User Management System</h2>
            <p>Use the navigation bar to add or view users.</p>
        </div>
    );
};

export default Home;
